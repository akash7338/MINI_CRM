// Root build.gradle for backend services
plugins {
    id 'java'
}

allprojects {
    group = 'com.minigenesys'
    version = '0.0.1-SNAPSHOT'

    repositories {
        mavenLocal()
        mavenCentral()
    }
}
