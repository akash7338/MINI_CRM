<!--
  Purpose: Access Control List (ACL) configuration.
  Defines network subnet lists that can be used to allow or deny traffic for various
  modules (like mod_sofia or mod_event_socket).
  Our 'lan' list uses a default-deny policy and explicitly permits only localhost
  and private/Docker subnet spaces to protect FreeSWITCH from public access.
  Classless Inter-Domain Routing (CIDR) notation specifies the IP address range.
-->
<configuration name="acl.conf" description="Network Lists">
  <network-lists>
    <list name="lan" default="deny">
      <!-- Localhost / Loopback: Allows local CLI connections inside the container -->
      <node type="allow" cidr="127.0.0.1/32"/>
      <!-- Class A Private Network: Common corporate VPN / Cloud VPC subnets -->
      <node type="allow" cidr="10.0.0.0/8"/>
      <!-- Class B Private Network / Docker Subnets: Allows communication from other Docker containers on the bridge network -->
      <node type="allow" cidr="172.16.0.0/12"/>
      <!-- Class C Private Network: Standard home / office LAN networks (allows host Mac machine connection) -->
      <node type="allow" cidr="192.168.0.0/16"/>
    </list>
  </network-lists>
</configuration>
